window.addEventListener('hashchange', function() {
    console.log('The hash has changed!');
    console.log('New hash:', window.location.hash);
    // You can add additional code here to handle the hash change
});